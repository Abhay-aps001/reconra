# Tasks 18–20 compressed frontend sprint

Implements the approved 2026-08-29 spec and Tasks 18–20 plan, with the user's compressed sprint scope. No Task 21, backend edits, commit, or push.

1. Inspect public run, exception, audit, stage and CSV contracts; lock deterministic test fixtures to those fields.
2. Write failing browser tests for demo, honest pending state, tie-out integrity, ledger evidence/filtering, review actions/conflicts, and audit navigation.
3. Implement validated API transport, same-origin rewrite from NEXT_PUBLIC_API_BASE_URL, and a lightweight session run context. Never optimistically alter money. Use BigInt for the conservation check over safe integer paise.
4. Build workspace and dominant tie-out rail, TanStack ledger with local scrolling and accessible evidence dialog, exception workbench and audit timeline. Keep operational surfaces clean and Task 17 palette unchanged.
5. Run unit/browser suites, lint, typecheck and build; inspect 1440/768/320 screenshots; attempt a real local backend smoke without provider credentials.

Contract limitations: only aggregate explained credit is public; do not infer rules/agent/human subtotals. Public exceptions omit hypothesis/confidence/candidate resolution. Show unavailable for these fields and link actual lifecycle audit evidence. Only actual stages in the response are complete; residual/verification steps absent from stage data must not be invented.
